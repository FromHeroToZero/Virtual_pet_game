from pet import Pet


if __name__ == '__main__':
    # Prompt the user to name their pet and load existing data if available
    # Use name to construct Pet object
    pet = Pet("Aira")

    # Loop for options
    # Handle user choice to feed the pet
    # Handle user choice to play with the pet
    # Handle user choice to groom the pet
    # Handle user choice to save the game and exit
    # Handle invalid user choice

    # Introduce a random event affecting the pet's status
